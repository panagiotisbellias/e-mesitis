name: Ant Build

on:
  push:
    branches: [ "master", "main", "development", "task/ci-cd-enhancements-run-tests-on-prs-enforce-code-quality-checks-6f2rh4wVrghwc3p6" ]
  pull_request:
    branches: [ "master", "main", "development" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17

      - name: Build project
        run: ant clean jar

      - name: Run tests
        run: ant test

      - name: Run PMD (code quality)
        run: ant pmd

      - name: Upload test results
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: build/test-results/
